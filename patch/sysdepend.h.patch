Description: SoftDevice の BLE イベント用に SWI2_EGU2_IRQHandler のハンドラ宣言
 を追加する。
Author: koh aiaida <koh@aiaida.jp>
Origin: PMC 362_mbit_mtk3 (micro:bit v2 用 μT-Kernel 3.0、パーソナルメディア株式会社配布)
License-of-patch: この差分（追加・変更行）自体は koh aiaida が Apache-2.0 で提供する。
 適用対象のオリジナルファイルは PMC の配布条件（T-License 2.x）に従う。本パッチは
 PMC のソースを同梱するものではなく、利用者自身が入手した PMC 配布物に適用することを
 前提とする。
--- a/kernel/sysdepend/cpu/core/armv7m/sysdepend.h
+++ b/kernel/sysdepend/cpu/core/armv7m/sysdepend.h
@@ -36,6 +36,7 @@ IMPORT void Svcall_Handler(void);		/* Svcall */
 IMPORT void DebugMon_Handler(void);		/* Debug Monitor Handler */
 
 IMPORT void Default_Handler(void);		/* Default Handler (Undefine Interrupt) */
+IMPORT void SWI2_EGU2_IRQHandler(void);		/* SoftDevice event IRQ (SWI2) */
 
 /*
  * Dispatcher (dispatch.S)
  */
