Description: DEVCNF_USE_BLENUS 有効時に blenus デバイスドライバの定義を追加する。
Author: koh aiaida <koh@aiaida.jp>
Origin: PMC 362_mbit_mtk3 (micro:bit v2 用 μT-Kernel 3.0、パーソナルメディア株式会社配布)
License-of-patch: この差分（追加・変更行）自体は koh aiaida が Apache-2.0 で提供する。
 適用対象のオリジナルファイルは PMC の配布条件（T-License 2.x）に従う。本パッチは
 PMC のソースを同梱するものではなく、利用者自身が入手した PMC 配布物に適用することを
 前提とする。
--- a/device/include/device.h
+++ b/device/include/device.h
@@
 #if DEVCNF_USE_IIC		/* Use I2C communication device */
 #include "dev_i2c.h"
 #endif	/* DEVCNF_USE_IIC */
+
+#if DEVCNF_USE_BLENUS		/* Use BLE NUS device */
+#include "dev_blenus.h"
+#endif	/* DEVCNF_USE_BLENUS */
 
 #endif	/* USE_SDEV_DRV */
 #endif	/* __DEVINC_DEVICE_H__ */
